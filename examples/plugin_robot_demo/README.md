# plugin-robot-demo

Minimal pip-installable package showing RoboDeploy entry-point registration.

```bash
pip install -e .
robodeploy list-registry --discover
```

Expect `demo_arm` under robots and `demo_task` under tasks.
