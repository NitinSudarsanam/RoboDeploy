# RoboDeploy — Master Plan (Iteration 19)

**Canonical state**: `history.json`

## Goals

1. Improve CLI UX: allow omitting `--preset` when using `--dummy`.
2. Add unit tests for this behavior.
3. Record in `history.json`, commit, push.

## Subtasks

| ID | Title |
|----|--------|
| goal20-subtask1 | Make `--preset` optional when `--dummy` is set |
| goal20-subtask2 | Unit tests for missing --preset + --dummy |
| goal20-subtask3 | Update history.json; commit/push; cleanup |

