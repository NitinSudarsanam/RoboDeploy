# RoboDeploy — Master Plan (Iteration 21)

**Canonical state**: `history.json`

## Goals

1. Add structured JSON output mode for CLI commands that currently print a single path.
2. Keep defaults backward compatible (plain output unless `--json`).
3. Add unit tests; record in `history.json`; commit and push.

## Subtasks

| ID | Title |
|----|--------|
| goal22-subtask1 | Add `export-episode --json [--pretty]` structured output |
| goal22-subtask2 | Add tests for structured JSON output |
| goal22-subtask3 | Update history.json; commit/push; cleanup |

