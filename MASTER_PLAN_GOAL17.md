# RoboDeploy — Master Plan (Iteration 16)

**Canonical state**: `history.json`

## Goals

1. Add `robodeploy run-episode` for quick smoke runs without exporting datasets.
2. Ensure output is JSON and safe to serialize (no huge obs payloads).
3. Add unit tests, record in `history.json`, commit, push.

## Subtasks

| ID | Title |
|----|--------|
| goal17-subtask1 | Implement `run-episode` CLI command |
| goal17-subtask2 | Add unit test for JSON summary output |
| goal17-subtask3 | Update history.json; commit/push; cleanup |

