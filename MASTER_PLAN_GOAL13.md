# RoboDeploy — Master Plan (Iteration 12)

**Canonical state**: `history.json`

## Goals

1. Add a minimal `robodeploy` CLI for quick introspection and smoke tasks.
2. Provide `pyproject.toml` entry point (`[project.scripts]`) and tests.
3. Record outcome in `history.json` and keep docs trimmed (history is canonical).

## Subtasks

| ID | Title |
|----|--------|
| goal13-subtask1 | Add CLI commands: list-presets, list-registry, export-episode, serve-policy |
| goal13-subtask2 | Wire CLI entry point + add unit tests |
| goal13-subtask3 | Update history.json; commit/push; doc cleanup |

