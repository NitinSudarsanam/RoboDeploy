# RoboDeploy — Master Plan (Iteration 18)

**Canonical state**: `history.json`

## Goals

1. Document the CLI “power user” flags (`--custom-module`, `--discover`, `--action`, `--dummy`) in `README.md`.
2. Add a tiny example that exercises CLI smoke paths without any simulator.
3. Record in `history.json`, commit, push.

## Subtasks

| ID | Title |
|----|--------|
| goal19-subtask1 | Update README CLI docs with common patterns |
| goal19-subtask2 | Add `examples/cli_smoke_no_sim.py` |
| goal19-subtask3 | Update history.json; commit/push; cleanup |

