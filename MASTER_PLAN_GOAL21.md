# RoboDeploy — Master Plan (Iteration 20)

**Canonical state**: `history.json`

## Goals

1. Make JSON outputs consistent across CLI commands via `--pretty`.
2. Ensure JSON outputs remain parseable and stable (sorted keys in pretty mode).
3. Add unit tests; record in `history.json`; commit and push.

## Subtasks

| ID | Title |
|----|--------|
| goal21-subtask1 | Add `--pretty` to JSON-producing CLI commands |
| goal21-subtask2 | Add tests that parse JSON outputs |
| goal21-subtask3 | Update history.json; commit/push; cleanup |

