# RoboDeploy — Master Plan (Iteration 22)

**Canonical state**: `history.json`

## Goals

1. Add structured JSON output mode for `robodeploy run-episode` via `--json`.
2. Keep defaults backward compatible (plain info JSON unless `--json`).
3. Add unit tests; record in `history.json`; commit and push.

## Subtasks

| ID | Title |
|----|--------|
| goal23-subtask1 | Add `run-episode --json [--pretty]` structured output |
| goal23-subtask2 | Add tests for `run-episode --json` output |
| goal23-subtask3 | Update history.json; commit/push; cleanup |

