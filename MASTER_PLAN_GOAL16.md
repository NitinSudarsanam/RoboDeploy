# RoboDeploy — Master Plan (Iteration 15)

**Canonical state**: `history.json`

## Goals

1. Make `robodeploy list-registry` usable for user projects by supporting `--custom-module`.
2. Add a regression test that imports an example user module and verifies its components appear in the registry output.
3. Record in `history.json`, commit, push.

## Subtasks

| ID | Title |
|----|--------|
| goal16-subtask1 | Add `list-registry --custom-module` support |
| goal16-subtask2 | Add unit test using `examples.user_kuka_sinusoid.components` |
| goal16-subtask3 | Update history.json; commit/push; cleanup |

