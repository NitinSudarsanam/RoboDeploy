# RoboDeploy — Master Plan (Iteration 17)

**Canonical state**: `history.json`

## Goals

1. Add policy-free action injection (`--action`) for CLI smoke paths.
2. Support this in both `run-episode` and `export-episode`.
3. Add unit tests; record in `history.json`; commit and push.

## Subtasks

| ID | Title |
|----|--------|
| goal18-subtask1 | Add `--action` to CLI and implement action_fn generator |
| goal18-subtask2 | Add unit tests for action mode |
| goal18-subtask3 | Update history.json; commit/push; cleanup |

