backend: ros2
task: benchmark_reach_target
policy: benchmark_reach_scripted
task_kwargs:
  target_q: 0.4
  success_tol: 0.08
  max_steps: 300
policy_kwargs:
  target_q: 0.4
  ramp_rate: 0.06
max_episode_steps: 300
sim2real_pair: kuka_reach_real
custom_modules:
  - benchmarks.manipulation_v1.reach_target.task
